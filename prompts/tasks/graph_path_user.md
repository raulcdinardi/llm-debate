You are given a directed weighted graph with a reported heuristic h(n).

{graph_text}

Task:
- Choose a prefix path of length K starting from Start.
- Output ONLY JSON of the form {{"path":["A","B","C"]}} with exactly K nodes.
- The path lists the nodes visited AFTER Start (do not include Start).
- Use only the listed node names.
