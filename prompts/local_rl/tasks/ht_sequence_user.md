Simulate a fair coin by giving a sequence of exactly {sequence_len} flips.

Use only H or T, separated by comma+space, following this output pattern:
H, T, H, ...

Output only the sequence and nothing else.
